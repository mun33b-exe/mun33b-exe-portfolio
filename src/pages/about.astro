---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import SectionHeader from '../components/SectionHeader.astro';
import { skills } from '../data/skills';
import { getSkillIcon, getCategoryIcon } from '../data/skillIcons';
---

<BaseLayout title="About | Muhammad Muneeb Ur Rehman">
  <Navbar />
  
  <main class="pt-24 pb-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <SectionHeader 
        title="About Me" 
        subtitle="Mobile app developer passionate about building beautiful, performant applications"
      />

      <div class="grid lg:grid-cols-2 gap-16 items-start">
        <!-- Bio Section -->
        <div class="space-y-6">
          <div class="prose prose-invert max-w-none">
            <p class="body-lg text-secondary leading-relaxed">
              Hi, I'm <span class="text-accent font-semibold">Muhammad Muneeb Ur Rehman</span>, 
              a mobile app developer from Pakistan with a passion for creating 
              seamless, pixel-perfect user experiences.
            </p>
            
            <p class="body-md text-secondary leading-relaxed">
              I specialize in <span class="text-primary">Flutter development</span>, building 
              cross-platform applications that look and feel native on both iOS and Android. 
              My focus is on <span class="text-primary">clean architecture</span>, state management, 
              and maintainable code that scales.
            </p>
            
            <p class="body-md text-secondary leading-relaxed">
              From implementing complex UI animations to integrating Firebase services and 
              REST APIs, I enjoy solving challenging problems and delivering polished products. 
              I believe in writing code that's not just functional, but elegant.
            </p>

            <p class="body-md text-secondary leading-relaxed">
              When I'm not coding, you'll find me exploring new mobile development patterns, 
              experimenting with UI/UX concepts, or contributing to the developer community.
            </p>
          </div>

          <!-- Quick Facts -->
          <div class="grid grid-cols-2 gap-4 pt-6">
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">1+</p>
              <p class="text-secondary text-sm">Years Experience</p>
            </div>
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">10+</p>
              <p class="text-secondary text-sm">Apps Developed</p>
            </div>
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">Flutter</p>
              <p class="text-secondary text-sm">Primary Stack</p>
            </div>
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">Clean</p>
              <p class="text-secondary text-sm">Architecture Focus</p>
            </div>
          </div>
        </div>

        <!-- Skills Section -->
        <div class="space-y-6">
          <h3 class="heading-md text-primary">Core Expertise</h3>

          <!-- Dynamically render skill categories -->
          {skills.slice(0, 4).map((skillGroup) => (
            <div class="p-5 bg-surface rounded-2xl border border-surface hover:border-accent/30 transition-all duration-300 group">
              <h4 class="text-base font-semibold text-accent mb-3 flex items-center gap-3">
                <span class="w-7 h-7 flex items-center justify-center bg-accent/10 rounded-lg text-accent" set:html={getCategoryIcon(skillGroup.category)} />
                {skillGroup.category}
              </h4>
              <div class="flex flex-wrap gap-2">
                {skillGroup.items.map((item) => (
                  <span class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm bg-background text-secondary rounded-lg border border-surface group-hover:border-accent/20 transition-colors">
                    <span class="w-3.5 h-3.5 opacity-60" set:html={getSkillIcon(item)} />
                    {item}
                  </span>
                ))}
              </div>
            </div>
          ))}

          <!-- Show remaining categories in a smaller format -->
          <div class="grid grid-cols-2 gap-4">
            {skills.slice(4).map((skillGroup) => (
              <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors group">
                <h4 class="text-sm font-semibold text-accent mb-2 flex items-center gap-2">
                  <span class="w-5 h-5 opacity-70" set:html={getCategoryIcon(skillGroup.category)} />
                  {skillGroup.category}
                </h4>
                <p class="text-xs text-secondary">
                  {skillGroup.items.slice(0, 3).join(' • ')}
                </p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>
