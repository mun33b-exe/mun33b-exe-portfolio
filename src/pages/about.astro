---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import SectionHeader from '../components/SectionHeader.astro';
import { skills } from '../data/skills';
import { getSkillIcon, getCategoryIcon } from '../data/skillIcons';
---

<BaseLayout title="About | Muhammad Muneeb Ur Rehman">
  <Navbar />
  
  <main class="pt-24 pb-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <SectionHeader 
        title="About Me" 
        subtitle="Flutter-focused mobile app developer with a foundation in clean architecture"
      />

      <div class="grid lg:grid-cols-2 gap-16 items-start">
        <!-- Bio Section -->
        <div class="space-y-6">
          <div class="prose prose-invert max-w-none space-y-5">
            <!-- Intro -->
            <p class="body-lg text-secondary leading-relaxed">
              I'm <span class="text-accent font-semibold">Muhammad Muneeb Ur Rehman</span>, 
              a Flutter-focused mobile app developer from Pakistan. I work on building 
              structured, real-world mobile applications with a strong focus on clean 
              architecture and predictable state management.
            </p>
            
            <!-- Core Focus -->
            <p class="body-md text-secondary leading-relaxed">
              My main expertise lies in <span class="text-primary">Flutter development</span> using 
              <span class="text-primary">BLoC and Clean Architecture</span>. I prioritize writing 
              maintainable, well-structured code that is easy to scale and reason about, especially 
              in medium-sized production-style applications.
            </p>
            
            <!-- Backend Support -->
            <p class="body-md text-secondary leading-relaxed">
              Alongside Flutter, I handle backend and cloud integrations for mobile apps, including 
              REST APIs built with Node.js and Express, as well as Firebase and Supabase for 
              authentication, data storage, and notifications.
            </p>

            <!-- Practical Experience -->
            <p class="body-md text-secondary leading-relaxed">
              I've worked on projects such as chat applications, service booking systems, and 
              data-driven mobile apps, where I was responsible for UI implementation, state 
              management, and backend API integration.
            </p>

            <!-- Career Intent -->
            <p class="body-md text-secondary leading-relaxed">
              I'm currently <span class="text-accent">open to internships, junior roles, and freelance 
              opportunities</span> where I can contribute to production-level Flutter applications 
              and continue strengthening my engineering fundamentals.
            </p>

            <!-- Personal Note -->
            <p class="body-sm text-meta leading-relaxed italic">
              Outside of projects, I enjoy exploring Flutter patterns and improving app structure and usability.
            </p>
          </div>

          <!-- Quick Facts -->
          <div class="grid grid-cols-2 gap-4 pt-6">
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">1+</p>
              <p class="text-secondary text-sm">Years Experience</p>
            </div>
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">10+</p>
              <p class="text-secondary text-sm">Apps Developed</p>
            </div>
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">Flutter</p>
              <p class="text-secondary text-sm">Primary Stack</p>
            </div>
            <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors">
              <p class="text-accent font-semibold text-2xl">Clean</p>
              <p class="text-secondary text-sm">Architecture Focus</p>
            </div>
          </div>
        </div>

        <!-- Skills Section -->
        <div class="space-y-6">
          <h3 class="heading-md text-primary">Core Expertise</h3>

          <!-- Dynamically render skill categories -->
          {skills.slice(0, 4).map((skillGroup) => (
            <div class="p-5 bg-surface rounded-2xl border border-surface hover:border-accent/30 transition-all duration-300 group">
              <h4 class="text-base font-semibold text-accent mb-3 flex items-center gap-3">
                <span class="w-7 h-7 flex items-center justify-center bg-accent/10 rounded-lg text-accent" set:html={getCategoryIcon(skillGroup.category)} />
                {skillGroup.category}
              </h4>
              <div class="flex flex-wrap gap-2">
                {skillGroup.items.map((item) => (
                  <span class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm bg-background text-secondary rounded-lg border border-surface group-hover:border-accent/20 transition-colors">
                    <span class="w-3.5 h-3.5 opacity-60" set:html={getSkillIcon(item)} />
                    {item}
                  </span>
                ))}
              </div>
            </div>
          ))}

          <!-- Show remaining categories with consistent tag format -->
          <div class="grid grid-cols-2 gap-4">
            {skills.slice(4).map((skillGroup) => (
              <div class="p-4 bg-surface rounded-xl border border-surface hover:border-accent/30 transition-colors group">
                <h4 class="text-sm font-semibold text-accent mb-3 flex items-center gap-2">
                  <span class="w-5 h-5 opacity-70" set:html={getCategoryIcon(skillGroup.category)} />
                  {skillGroup.category}
                </h4>
                <div class="flex flex-wrap gap-1.5">
                  {skillGroup.items.slice(0, 4).map((item) => (
                    <span class="inline-flex items-center gap-1 px-2 py-1 text-xs bg-background text-secondary rounded-md border border-surface group-hover:border-accent/20 transition-colors">
                      <span class="w-3 h-3 opacity-60" set:html={getSkillIcon(item)} />
                      {item}
                    </span>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>
